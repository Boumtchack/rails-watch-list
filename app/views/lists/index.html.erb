<h1>Voici vos Listes</h1>
<div class="container w-75 px-0 h-75">
  <ul class="list-group w-100 d-flex justify-content-evenly">
    <% @lists.each do |list| %>
    <li class="list-group-item"><%= link_to list.name, list_path(list) %></li>
    <% end %>
  </ul>
</div>
